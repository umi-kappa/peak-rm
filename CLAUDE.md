# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの現状

このリポジトリは**仕様策定フェーズ**であり、実装コード・ビルド設定・テストは未存在。実体は `docs/spec.md` のみ。技術スタック（フレームワーク、言語、ストレージ実装など）は未確定で、Issue `#1`（ブランチ `issue/1-add-spec-md`）で仕様の叩き台を整備している段階。

実装を始める前に技術選定・ディレクトリ構成・ツールチェーン（ビルド/テスト/Lint）を決める必要がある。これらが決まったら本ファイルに追記する。

## プロダクトの本質（実装判断の指針）

PeakRM は「1RM の成長を可視化する筋トレアプリ」だが、UI/UX の判断はすべて以下のコンセプトに従う。仕様 (`docs/spec.md`) を読む際もこの軸でブレないこと。

- **計画通りに実行させる（甘え防止）**: トレーニング中の意思決定を排除するのが核心価値。利便性のために選択肢を増やす変更は、このコンセプトと衝突しうるため要注意。
- **「入力」ではなく「実行」にフォーカス**: 操作ステップは最小化。確認ダイアログや設定変更導線をトレーニング画面に持ち込まない。
- **重量・メニューはトレーニング中変更不可**: これは UX 上の制約ではなく**設計上の不変条件**。実装でも UI から到達できないようにする（単に disabled にするのではなく状態モデルで担保）。
- **記録するのは実績のみ**: セットスキップ・セッション中断も「記録対象」であり、なかったことにはしない。

## 仕様上の重要な決定事項と未決事項

実装時に迷いがちなポイント。`docs/spec.md` で**明示的に確定している制約**:

- 対象種目はベンチプレス／スクワット／デッドリフトの **3 種目のみ**（種目追加は非対応）
- データ保存は**ローカル**（LocalStorage または IndexedDB）。ログイン／クラウド同期は実装しない
- 1RM グラフは**日付ごとに 1 データ**。同日複数トレーニング時は**後の記録で上書き**（追記ではない）
- セット完了でインターバルタイマーが**自動開始**。インターバル値は事前設定値を使用し、手動停止のみ可能

**未決事項**（実装着手前に決定が必要）:

- 1RM の代表値計算方法（仕様書に「後で決定」と明記）
- ストレージ実装の選択（LocalStorage か IndexedDB か）

これらに勝手な仮定で実装を進めない。ユーザーに確認するか、仕様書を更新してから着手する。

## やらないこと（明示的にスコープ外）

機能追加の提案や実装を行う前に、以下が**意図的にスコープ外**であることを思い出すこと:

- ログイン／クラウド同期
- 種目の追加機能
- 達成率などの詳細分析
- 複雑な設定項目

「便利そう」を理由にこれらを足さない。シンプルさが最優先。

## ドキュメント

- `docs/spec.md` — 唯一の仕様ソース。実装前後で必ず参照・更新する。
- `README.md` — 一行紹介のみ。詳細は spec.md に集約。
