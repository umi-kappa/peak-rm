# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの現状

仕様策定が完了し、`docs/spec.md` に集約済み。実装コード・ビルド設定・テストはこれから整備するフェーズ。ブランチ `issue/1-add-spec-md`（Issue `#1`）で仕様を確定した。

## 技術スタック（確定済み）

詳細は `docs/spec.md` の「🧱 技術スタック」「💾 ストレージ」「🚀 デプロイ・公開」を参照。要点のみ:

- **フレームワーク**: Vue 3 + Vite + TypeScript（Composition API + `<script setup>`）
- **スタイリング**: scoped CSS（Tailwind は使わない）
- **グラフ**: vue-chartjs（Chart.js）
- **ストレージ**: IndexedDB（Dexie.js）
- **PWA**: vite-plugin-pwa（`registerType: 'autoUpdate'`、確認ダイアログ無し）
- **コンポーネント開発**: Storybook（Vue 3 + Vite）
- **テスト**: Vitest projects（ロジックは `happy-dom`、Storybook の play 関数は `@storybook/addon-vitest` + headless Chromium。すべて Vitest で実行）
- **Lint / Format**: ESLint + Prettier
- **ホスティング**: 本体は Cloudflare Pages、Storybook は Chromatic（`main` push で自動デプロイ）

## プロダクトの本質（実装判断の指針）

PeakRM は「1RM の成長を可視化する筋トレアプリ」だが、UI/UX の判断はすべて以下のコンセプトに従う。仕様 (`docs/spec.md`) を読む際もこの軸でブレないこと。

- **計画通りに実行させる（甘え防止）**: トレーニング中の意思決定を排除するのが核心価値。利便性のために選択肢を増やす変更は、このコンセプトと衝突しうるため要注意。
- **「入力」ではなく「実行」にフォーカス**: 操作ステップは最小化。確認ダイアログや設定変更導線をトレーニング画面に持ち込まない（例外: 破壊的操作のスキップ・中断のみ確認を許容）。
- **重量・メニューはトレーニング中変更不可**: これは UX 上の制約ではなく**設計上の不変条件**。実装で `Session.menu` を `Readonly<MenuPreset>` 型として deep copy で焼き込み、トレーニング画面は `Session.menu` のみを参照する（`menu_presets` を直接見ない）。
- **記録するのは実績のみ**: セットスキップ・セッション中断も「記録対象」であり、なかったことにはしない。
- **想定ユーザー**: 作者本人および同じ価値観を持つ個人トレーニー。主に linear progression が機能する初心者〜中級者期間が対象。中上級者向け autoregulation・ピリオダイゼーション・補助種目はスコープ外。

## 仕様上の重要な決定事項

実装時に迷いがちなポイント。`docs/spec.md` で**明示的に確定している制約**:

### コア仕様

- 対象種目はベンチプレス / スクワット / デッドリフトの **3 種目のみ**（種目追加は非対応）
- 1 セッション = **1 種目**。**1 日 1 セッション**が前提（同日複数セッションは想定しない、発生したら最新で上書き）
- データ保存は **IndexedDB（Dexie.js）**。ログイン / クラウド同期は実装しない
- 重量は **kg のみ、0.25 kg 刻み**

### 1RM 推定式（FWJ 換算式）

- ベンチプレス: `1RM = w × (1 + r / 40)`
- スクワット / デッドリフト: `1RM = w × (1 + r / 33.3)`
- 有効レンジは **1〜12 reps**。それを超える入力は受け付けるが推定精度は低下
- スキップ（実績 0 回）は **1RM 計算から除外**（記録には残す）
- 1RM グラフは**日付ごとに 1 データ**。同日複数トレーニング時は最新で上書き（デバイスのローカルタイムゾーン基準）

### Linear progression（常時有効）

- トリガー: **同一種目の直前セッション**が `executed`（= 全セットで `actualReps ≥ targetReps` を満たして終了）
- 増量幅: ベンチ `+2.5 kg` / スクワット・デッドリフト `+5 kg`
- ベースラインは `menu_presets[exercise].weight`（手動編集は累積する）
- 失敗 / 中断時は据え置き。連続据え置き時の自動デロード提案は実装しない
- 初回起動・データクリア・Import 直後は増量を適用しない

### タイマー・iOS Safari 対応

- セット完了でインターバルタイマー**自動開始**。事前設定値を使用、手動停止のみ可能
- 0 秒到達時は **音のみ** で通知（バイブ・画面遷移なし）。0 秒到達後もカウントは止めず、超過秒数を `+秒数` で表示（延長ボタンは設けない）
- トレーニング画面表示中は **Screen Wake Lock API** で画面スリープを防ぐ
- **AudioContext** はメニュー設定の「開始」ボタンタップ時（ユーザージェスチャ内）に生成・`resume()`（iOS Safari 制約）

### セッション永続化

- セッション開始時に `status = 'aborted'`（保守的デフォルト）で insert。セット完了ごとに増分 update
- 最終セット完了で `status = 'executed'` へ更新、中断時はそのまま `aborted` で確定
- タブクローズ / クラッシュ / SW autoUpdate のリロード時は `aborted` セッションとして履歴に残る
- 自動再開（resume）は行わない

### UI / 画面

- メニュー設定は種目ごとに最後の値を保持。初回起動時のみ全種目共通の初期値（**40 kg / 8 回 / 3 セット / 90 秒**）を表示
- セッション完了・中断後は **結果確認画面**を経由
- 結果確認画面の戻りボタンは遷移元によって動的（完了経由 → ホーム、履歴経由 → 履歴）
- **スキップ / 中断は確認ダイアログを振る**（両方とも破壊的操作のため）
- **設定画面**のスコープは **データ操作（Export / Import）のみ**。トレーニング挙動を変える設定は置かない
- データ Export / Import の JSON envelope は `{ schemaVersion: 1, exportedAt, sessions, menuPresets }`。Import は atomic（検証失敗時は既存データを保持）

これらに勝手な仮定で実装を進めない。仕様書を確認するか、ユーザーに相談する。

## デザイン

画面のビジュアルデザインは **Claude Design** で作成する。spec.md に記載の画面構成・遷移・UI 配置はデザイン作成過程で変更される可能性がある。機能仕様・データモデル・1RM 計算式・ストレージ設計などのコア仕様はデザイン変更に左右されない。

確定したデザイン成果物（デザイントークン・画面リファレンス・HTML プロトタイプ）は `docs/design/` を参照。実装時は `docs/design/README.md` のトークン仕様（色・タイポグラフィ・スペーシング）に厳密に従う。`docs/design/source/` の JSX は見た目の意図を示すリファレンスであり、Vue へ翻訳する際は値のみを写し取る（コードはコピーしない）。

**トーンガイド（コンセプト保護のため）**: 賞賛コピー・祝祭アニメーション・streak ゲーミフィケーション・onboarding carousel・カウントダウン ring 装飾・フレンドリーな絵文字多用は禁止。タイポグラフィ主導の中立な事実提示、高コントラスト・グレースケール寄りで設計する。詳細は spec.md「トーンガイド」節を参照。

## やらないこと（明示的にスコープ外）

機能追加の提案や実装を行う前に、以下が**意図的にスコープ外**であることを思い出すこと:

- ログイン / クラウド同期
- 種目の追加機能（Big3 以外の補助種目を含む）
- 達成率・ボリューム推移・RIR 推定などの詳細分析
- 複雑な設定項目（インターバル既定値変更・Linear progression on/off など、トレーニング挙動を変える設定）
- トレーニング中に新たな意思決定を生む UI（インターバル延長ボタン、重量変更、メニュー変更など）
- 自動デロード提案（プラトー時は手動でユーザーが下げる）
- セッション自動再開

「便利そう」を理由にこれらを足さない。シンプルさが最優先。

## ドキュメント・規約

- [docs/spec.md](docs/spec.md) — 唯一の仕様ソース。実装前後で必ず参照・更新する
- [docs/conventions.md](docs/conventions.md) — コーディング・命名・テスト・スタイル・ドキュメント表記の規約。実装・ドキュメント編集の前に必ず参照する
- [docs/design/README.md](docs/design/README.md) — デザイントークン（色・タイポグラフィ・スペーシング）
