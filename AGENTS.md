# AGENTS.md

このファイルは、PeakRM で作業する AI コーディングエージェント向けの共通ガイドである。変化しやすい仕様や進捗をここへ複製せず、各正本を参照する。

## 正本

- 機能仕様・データモデル・設計判断: `docs/spec.md`
- 実装・命名・アクセシビリティ・テスト・スタイル規約: `docs/conventions.md`
- UI・デザイントークン・画面リファレンス: `docs/design/README.md`
- セットアップと利用可能なコマンド: `README.md` と `package.json`
- 進捗と残作業: GitHub Issues

機能や挙動を変更する前に `docs/spec.md` の該当箇所を、コードを変更する前に `docs/conventions.md` の該当箇所を読む。UI を変更するときは `docs/design/README.md` も読む。仕様と実装が矛盾している場合や、新しいプロダクト判断が必要な場合は、独自に補完せず作業を止めて相談する。

## プロダクトの不変条件

- 核心価値は「計画通りに実行させる（甘え防止）」。トレーニング中の意思決定を増やさない
- メニューは開始時にセッションへ deep copy で焼き込み、トレーニング中は変更させない
- スキップと中断もなかったことにせず、実績として記録する
- 対象はベンチプレス・スクワット・デッドリフトの3種目のみ。ログインやクラウド同期は追加しない
- 根幹を壊すエラーは握りつぶさず共通エラー境界へ流す。最善努力と仕様で定めた機能だけを局所的に縮退させる
- 利便性だけを理由に、仕様でスコープ外とされた機能を追加しない

詳細や例外は必ず `docs/spec.md` を優先する。

## 作業手順

1. 対象 Issue と関連する正本を確認する
2. `rg` で既存の類似実装、呼び出し元、co-located なテストや Story を探す
3. 既存の責務分割と命名に合わせ、必要な範囲だけ変更する
4. 挙動を変える場合は、実装と同じ変更内でテストまたは Story の `play` を更新する
5. 変更種別に応じた検証を実行し、差分を確認してから完了を報告する

既存のユーザー変更は保持する。対象外のリファクタリングや依存追加を混ぜない。仕様変更が必要なら、コードだけでなく正本も同じ変更内で更新する。

## 実装の案内

- 純粋なドメインロジック: `src/core/`
- IndexedDB / Dexie の永続化: `src/storage/`
- 共有・画面専用ロジック: `src/composables/shared/`、`src/composables/pages/`
- UI プリミティブと複合コンポーネント: `src/components/shared/`
- 画面専用コンポーネント: `src/components/pages/`
- 画面エントリ: `src/pages/`
- テストと Story は対象ファイルの隣に置く

より詳しい依存方向と配置規則は `docs/conventions.md` を参照する。

## 検証

変更に直接対応する最小の検証から実行し、必要に応じて範囲を広げる。

- すべての変更: `npm run format:check`
- TypeScript / Vue: `npm run lint` と `npm run typecheck`
- ドメインロジックや永続化: 関連する `*.spec.ts` を実行後、`npm test`
- コンポーネントや画面: 関連 Story の `play` を確認後、`npm run test:storybook`
- アプリの挙動、ルーティング、ビルド構成に影響する変更: `npm test` と `npm run build`

テストを追加しない場合は、挙動変更がない理由と代替の検証を明示する。動作確認なしで完了扱いにしない。
